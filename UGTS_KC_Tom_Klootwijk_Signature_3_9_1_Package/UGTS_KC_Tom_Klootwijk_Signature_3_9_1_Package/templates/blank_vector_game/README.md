# Blank Vector Game Template

Copy this directory, edit `project.json`, validate it and build:

```bash
PYTHONPATH=../../src python -m ugts_kc3 validate project.json
PYTHONPATH=../../src python -m ugts_kc3 build-web project.json dist
```
