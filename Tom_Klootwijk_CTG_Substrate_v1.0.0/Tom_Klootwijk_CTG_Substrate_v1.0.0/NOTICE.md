# Notice and evidence boundary

This package defines the **Tom Klootwijk Chrono-Topological-Geometric Substrate and Manifold** as a bounded formal specification derived from requester-supplied source documents and the supplied KLB SeedChain GPU 0.3.0 implementation.

Requester-supplied attribution:

- Name: Tom Klootwijk
- Identifier: NL200678942
- Date string: 10-07-1990
- Country marker: NL

The attribution is provenance metadata supplied by the requester. It has not been independently verified and is not a legal determination of identity, authorship, ownership, priority, patentability, or copyright status. The identifier and date are not mathematical invariants, geometry parameters, encryption keys, or physical constants.

The formal core treats topology as explicit gluing/routing, time as an explicit axis plus deterministic flow and certified events, and `SDF = 0` as a zero-level set. Generic implicit fields are not automatically exact signed-distance functions. A single parity or LSB bit may select a declared route or map; it does not change a multi-bit numeric value by itself. Compact procedural state trades storage for reconstruction work and does not imply zero VRAM, constant-time rendering, infinite resolution, or physical causation.

KLSC1 and KLOC1 use FNV-1a hashes for deterministic integrity checks. The formal definition registry in this package uses SHA-256 content addresses. Neither mechanism is a legal signature or proof of authorship.
