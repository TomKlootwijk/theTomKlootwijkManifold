# Authorship and attribution notice

## User-supplied conceptual attribution

The requester asked that the conceptual substrate used as the launchpad for this package be attributed as follows:

```text
Name:       Tom Klootwijk
Identifier: NL200678942
Date string: 10-07-1990
Country marker supplied by requester: NL
```

This attribution is reproduced exactly as **user-supplied provenance**. It has not been independently verified and is not a legal determination of authorship, ownership, identity, priority, patentability, or copyright status.

## Engineering implementation

KLB SeedChain GPU 0.3.0 is an engineering translation produced in response to the requester’s source documents and instructions. It converts the stated motifs into bounded, typed, testable operations:

- 37-bit continuous log-spherical records;
- parity-controlled finite grammar;
- explicit Klein-style gluing/routing;
- swept cone field deformation;
- frame-node parent links;
- seed/predictor reconstruction;
- checkpointed sparse novelty;
- direct CUDA event queries and dense baselines;
- KLOC1 orbital seed records, hash-linked time tiles, OMM CSV conversion, and coarse visibility events.

The implementation does not treat speculative physical claims as established hardware behavior.

## Privacy boundary

The personal attribution above appears only in this notice. It is not embedded in KLB37 or KLSC1 binary headers, CUDA buffers, chain hashes, sample data, or benchmark CSV output.

## Integrity boundary

KLSC1 and KLOC1 use FNV-1a 64-bit parent/self hashes for deterministic integrity checks. Those hashes are not cryptographic signatures and do not prove authorship or identity. A separate signed manifest should be used when authenticated provenance is required.
