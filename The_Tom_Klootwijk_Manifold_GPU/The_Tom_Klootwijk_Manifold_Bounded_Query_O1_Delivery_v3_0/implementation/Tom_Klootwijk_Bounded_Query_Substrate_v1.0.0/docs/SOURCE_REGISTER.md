# Source register

The following requester-supplied files were used as the technical basis. They are
referenced by SHA-256 and are not redistributed in this clean implementation.

```text
klb_seedchain_gpu_v0.3.0(4).zip
SHA-256 c3e00e94ff1c3c13ac9ae76ea84a95117551e022375cefda8c706a7bb14d1ad4

UGTS_KC_2_0_Tom_Klootwijk_Expanded_Substrate(2).pdf
SHA-256 a29dd9959ebd300c32d0e1df8fa2c0d4ee15f428892557b98b5f18cd3e878399
```

Source-derived architecture retained:

```text
finite grammar + typed seed/state
-> local support
-> compatibility
-> certified guard classification
-> verified transition/reset
-> topology invariants + lineage/novelty
```

The supplied package's MIT license permitted reuse and modification of the
engineering concepts and code patterns. This package is a clean bounded-profile
implementation rather than a byte-for-byte fork.
